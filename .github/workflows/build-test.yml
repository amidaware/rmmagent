name: Run tests on all

on:
  push:
    branches: [ BigRefactor ]

jobs:
  build:
    runs-on: [ self-hosted ]
    steps:
    - uses: actions/checkout@v2
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.18.3
    - name: Test
      run: go test -v ./...